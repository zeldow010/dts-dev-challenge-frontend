<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/global.css">
    <title>HMCTS Task Management System</title>
</head>
<body>
    <h1>HMCTS | <%= task.title %></h1>
<hr>
<main>
    <div class="title_section">
        <h1></h1>
        <div class="tag">
            <%= task.task_status %>
        </div>
        <em><%= task.due.date %></em>
    </div>
    <p><%= task.description %></p>
    <div class="task_options">
        <form action="/tasks/<%= task.task_id %>/delete" method="post">
            <button type="submit">Delete Task</button>
        </form>
        <form action="/tasks/<%= task.task_id %>/next_status" method="post">
            <button type="submit" <%  if (task.task_status == "Completed") { %>disabled<% }; %>><%=
                task.task_status == "Draft" ? "Publish" :
                "Mark as Done"
            %></button>
        </form>
    </div>
</main>
</body>
</html>